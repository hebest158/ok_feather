apiVersion: v1
kind: Service
metadata:
  name: tiger
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tiger"
    port: 8080
  selector:
    app: tiger
