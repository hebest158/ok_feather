apiVersion: v1
kind: Service
metadata:
  name: lion
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lion"
    port: 8080
  selector:
    app: lion
